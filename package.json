{
  "name": "medical-asset-generator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "start:api": "npm run start -w apps/api"
  }
}
